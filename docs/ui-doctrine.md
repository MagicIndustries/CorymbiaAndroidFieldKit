# UI doctrine

A living document, added to as design decisions are made. Every screen and component in the
Corymbia Field Kit is checked against the rules below. This is the answer to the question the
whole design system exists to serve: how screens and components should be built so that, once
a design is settled, it is shared across tools within this app and reused in any future
Corymbia app rather than reinvented per screen.

## Established rules

1. **Single-focus field screens.** One job per screen, one obvious primary action,
   everything else subordinate or off-screen.
2. **Capture is a mode, not a form.** Entering it is visually unmistakable — the screen
   changes enough that there is no doubt a capture is in progress.
3. **Progressive disclosure with a floor of zero.** GPS-only is a complete, valid record.
   Title is an easy next step; description and media a step beyond. Every level is a
   legitimate stopping point.
4. **Nothing blocks capture.** No modal question, no required field, no waiting on a fix,
   ever stands between opening the application and recording a position.
5. **Consistent input affordances.** Text, voice and photo each have one visual signature,
   used identically everywhere, in the same order.
6. **Plain language for process, technical language for science.** Ecology, surveying and
   biology terms are correct and welcome. Software terms are not.
7. **Help is always adjacent.** A tappable help affordance per field, or per screen where
   per-field would be noise. Popovers, never hover — there is no hover in the field.
8. **Generous targets.** Sized for gloves, movement, and small hands.
9. **Colour never carries meaning alone.** Every status colour is backed by a word, a
   number, or a border style. Required for colour-vision deficiency and for direct sunlight.
10. **Hero names clamp to two lines, then clip.** Cards never grow with their content, so
    primary actions never move.
11. **Chips truncate in the middle, not the end.** Field project names are front-loaded with
    the site and back-loaded with what distinguishes them; tail truncation discards the
    useful half.
12. **Long names get an optional short label**, user-controlled. When left blank, the full
    name is used as-is (and truncated by the component, per rules 10/11) rather than
    shortened automatically — no heuristic shortens an ecologist's project name better than
    she does, and a bad automatic guess is worse than a clean ellipsis. Used in app bars,
    chips, batch lists and exported filenames.
13. **The full name is always one tap away**, via the standard help popover.
14. **Never scroll or animate a name.** Marquee text is unreadable while walking.
15. **Destructive actions are soft and reversible.** Deletion flags; files survive until a
    deliberate purge in settings. **The purge is not built** — no settings route, no orphan
    reconciliation, nothing anywhere in the application that deletes a media file except
    `useAttachMedia`'s rollback of a save it has just made. Until a later plan builds it, the
    first half of this rule holds and the second describes an intention: removed attachments'
    bytes and crash-orphaned files accumulate permanently (`docs/media-storage.md` §5).
    Nothing user-facing may say otherwise — see rule 6, and the removal confirmation named in
    the rule 7 row below.
16. **Every screen carries a spoken description, every action a speakable name.**
17. **A capture is three states, not one screen with extra widgets.** _Ready_ is the
    screen's ordinary self. _Acquiring_ — from the tap until the fix is settled — collapses
    to a single-focus view: the position, its grade, the time remaining, the dial, the
    words saying what is happening, and the one control. Nothing else is on screen.
    _Recorded_ returns the fuller view in a state that reads as finished: what was saved,
    its final accuracy, that it will not change again, and the two ways onward — capture
    again, or leave. This is rules 1 and 2 made concrete for the one interaction the
    product exists for; a capture screen that merely grows a countdown readout has not
    entered a mode, it has added a widget.
18. **An affordance row shows a count where a kind can repeat, and a busy affordance is
    genuinely disabled, not silently inert.** A record can carry several photos or voice
    notes but only one title, so "Photo ✓" once four are attached is a worse answer than
    "Photo · 4" — and a kind that can only happen once still carries no count, because
    "Title · 1" is noise, not information. A tile mid-write (a photo being saved to a file
    and then a row) must refuse a second tap outright: an `onPress` that quietly returns
    early looks pressable and swallows the tap, which is worse than doing nothing visible
    at all, because it teaches her the tap didn't register when it did. Both facts are
    two-channel under rule 9 — a number or a word, never colour or dimness alone.
19. **Text entry is a surface of its own, and a save says so.** A text box appended to the
    bottom of a screen's own column is a box under the Android soft keyboard: the keyboard
    arrives exactly where the last thing in a column is, and she types blind. Any field she
    types into opens as its own surface — entered and left, not a column that shifts — with
    the input and the controls that act on it centred as one block in the space the keyboard
    leaves, not pinned to either edge of the surface. The first build of this rule put the
    input at the top, reasoning that a keyboard rising from the bottom could then never reach
    it; that was reported back from the field as its own fault ("it's a pain to shift from
    bottom of screen to top"), because she is holding the phone one-handed and the journey
    from the keys to the top of the screen and back is as costly as typing blind was. Such a
    surface opens ready to type — the keyboard up and the box focused, which on Android means
    asking for the focus once the surface's window exists rather than declaring `autoFocus` —
    and its save must land on the first tap. On that last point: a `ScrollView` anywhere above
    the surface **in the React tree** eats that first tap unless it carries
    `keyboardShouldPersistTaps="handled"`, and it does so even when the surface is a `Modal` in
    a window of its own, because React Native routes touches by the React tree and not the
    native one. And a save that landed must say so where she is looking when that
    surface goes away: the surface closing is not the confirmation, because a cancel closes
    it identically. Two channels under rule 9 — a sentence naming what was written, and the
    affordance's own `✓` — never the value appearing somewhere further down the page, which
    is the failure this rule was written from. This does not soften rule 4: rule 4 forbids a
    modal that stands _between_ her and a recorded position, and every field this rule covers
    is one she chooses to open after the position is already saved. A surface that opened
    before a capture, or that a capture had to pass through, would still be forbidden.

20. **An outcome she cannot reconstruct must be recoverable, not merely displayed.** Every
    message this application shows about a *write* reports what happened to a piece of her
    work — "The photo could not be saved: disk full", "The recording stopped on its own and
    was not saved", "That recording was too short to carry anything", "The name was not
    saved: … The point itself is safe". She reads them one-handed, outdoors, mid-survey, and
    dismissing something is close to reflexive. A dismissible alert is therefore the wrong
    home for any of them: once tapped away the information is gone, and nothing in the
    application remembers it. The voice note is the sharpest case — the recording really is
    gone, and that sentence is the only thing that will ever say so, so a reflexive dismissal
    sends her away believing she has a note she does not have.

    Two consequences, and the second is the one with teeth. First, such a message renders on
    the screen it belongs to, never in a modal that takes it away — which is what the camera,
    voice, capture and launcher screens already do. Second, and not yet built: **anything
    substantive belongs in a record or a list she can go and look at.** A message she has
    walked away from should still be answerable later by opening something, rather than
    depending on her having read it at the moment it appeared.

    Note what that second half costs today. The event log (spec §8.5) is append-only and
    records `created`, `edited`, `media_added`, `filed`, `played`, `deleted` and `restored` —
    **it does not record failures at all**, and nothing in the application surfaces it to her
    in any case. So a failed save currently leaves no trace anywhere once its sentence is off
    screen. Closing that is tracked separately; until it is, transient messages must be
    treated as the only telling she gets, which is exactly why they must not be dismissible.

    An *alert* is still right for a question she is actively answering — the removal
    confirmation is modal-shaped for that reason, and is inline only because rule 15 wants her
    to see which photo she is discarding while she decides.

21. **An unbuilt destination gets no tile at all, rather than a disabled one.** A tool with
    nowhere to go is left off the launcher entirely: not dimmed, not badged "coming soon",
    not rendered pressable and inert. This is rule 3 applied to navigation, with rule 18 as
    its other parent. Rule 3 says every level of disclosure is a legitimate stopping point —
    a launcher carrying only the tools that exist *is* such a level, complete as it stands
    rather than a truncated view of a fuller one she is being shown the outline of. Rule 18
    says a control that looks pressable and quietly does nothing teaches her the tap did not
    register when it did; a badge explaining the silence is still a control that swallows the
    tap, and it spends screen space on a tool she cannot use. Between them they settle the
    case rule 18 stopped short of: the answer is not to disable the control, it is that the
    control should not be on the screen.

    **`ToolTiles`' `available` prop is the enforcement, not a convention to remember.**
    `packages/ui/src/launcher/ToolTiles.tsx` filters its ordering table against `available`
    and renders nothing for the rest, so a caller *cannot* ask for a disabled tile — there is
    no prop that would produce one. The ordering table still carries a row for every kind,
    including the two that are not built, so the day one of them gains a route the only
    change is a word added to the caller's list. `AVAILABLE_TOOLS` in
    `apps/fieldkit/app/index.tsx` is that list, and it is `capture` and `records` and nothing
    else: batching and a media library are absent for this reason and no other.

## How these rules are enforced

A rule enforced only by memory is a rule that erodes. Where a rule can be enforced by a
shared component or a lint rule, it must be, and the table below is checked against the code
as of this writing — see the commit history of this file for what changed and why.

| Rule                                    | Enforced by                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 4 — nothing blocks capture              | Not enforceable by a component or a lint rule, and still a review check in general — but the one place it is most at risk is now pinned by test. The duplicate guard (spec §9.5) is the rule's hardest case, because "a duplicate ought to be prevented" is a reasonable-sounding thought that quietly deletes the rule: accidental double-capture is a real field failure, but so is refusing a legitimate close-spaced pin, and two soil samples a metre apart are a normal thing to record. `apps/fieldkit/app/capture.tsx` therefore runs the guard in the `recorded` phase, _after_ `useCapture` has already written the row on the tap, so it can only ever be a warning about what just happened. The test `warns about a close-spaced pin and still records it — it never blocks` (`apps/fieldkit/app/__tests__/capture.test.tsx`) asserts the record's existence **before** the warning's presence, deliberately: a guard moved earlier — onto the tap, or onto the save — then fails on the record's absence, which is the sentence that names what actually went wrong. Verified by breaking the guard into a gate and watching it fail that way.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 5 — consistent input affordances        | `InputAffordanceRow` (`packages/ui/src/inputs/InputAffordanceRow.tsx`) renders the four affordances in a fixed order taken from the exported `INPUT_AFFORDANCE_ORDER` constant, so the order cannot drift screen to screen; asserted by test. **The exemption this row used to describe is resolved, not merely still granted.** An earlier state of this table recorded that the capture screen (`apps/fieldkit/app/capture.tsx`) reimplemented the row from scratch as a local `AffordanceTile`, because `InputAffordanceRow` had no _unavailable_ state and the two components rendered different affordances (title/description/voice/photo versus §9.6's location/title/voice/photo). Plan 4 is the branch that table row named as where this would attach, and it did: `AffordanceTile` is gone from the codebase, and `apps/fieldkit/app/capture.tsx`'s `RecordedAffordances` renders `InputAffordanceRow` itself, passing `completed`, `counts` and `busy` (rule 18) rather than reimplementing any of them. The location/description divergence resolved in the row's favour — location was never an input here, it is the fix the capture already made, shown by the dial and the accuracy readout above rather than a fifth tile that would do nothing when pressed (see `RecordedAffordances`'s own doc comment) — so the mismatch this row used to flag no longer exists on this screen.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 7 — help is adjacent, never hover       | `HelpAffordance` (`packages/ui/src/help/HelpAffordance.tsx`) is a tappable `?` that opens a modal. There is no `onHover`/tooltip code path in the component at all, so a hover-only help affordance cannot be built with it. **One exemption is granted, and it is recorded here rather than left looking like an oversight:** the capture screen (`apps/fieldkit/app/capture.tsx`) carries its help affordance in the _ready_ state only, explaining in plain language what one tap does and why standing still helps. Its _acquiring_ state deliberately carries none — rule 17 requires that nothing else is on screen while she stands still over a point, and a `?` beside the two numbers she is standing there for is exactly the "else". **Its _recorded_ state's exemption is spent, and this row records that rather than restating it.** That exemption was granted on the reasoning that the one thing the state asked for — a title — explained itself: the field is labelled ("A name for this point", as both the accessibility label and the placeholder), it is optional at every level (rule 3: GPS-only is a complete record), and there is nothing a `?` beside it could say that the box does not already say. It came with a pre-commitment, written into this row and into `capture.tsx`'s own comment: a later state on that screen that asked for something whose meaning is _not_ on its face — filing to an activity, **attaching media** — took a help affordance with it. The media-capture branch added attaching media to that state, so the exemption ended by its own terms and the affordance was added rather than the pre-commitment quietly reinterpreted: `capture-media-help`, in `RecordedAffordances`, beside the ADD TO THIS POINT label. What it says is what the four tiles and the strip cannot: that an attachment belongs to this one point rather than to the trip, that a voice tile plays back when tapped, that removing asks first because nothing here can undo it — and the fact nothing on screen can show her, that removing an attachment does not give the storage back, because there is no purge (rule 15). Only _acquiring_ is exempt now, by rule 17. Asserted by test in all three directions (present and opening in ready, absent in acquiring, present in recorded), so neither an affordance nor the remaining exemption can be removed silently.                                                                                                                                                                                                                                                                                                                                                                                               |
| 8 — generous targets                    | `Button` (`packages/ui/src/primitives/Button.tsx`) sets `minHeight` from the token scale: `touch.min` (48dp) for standard buttons, `field.control` (72dp) for field-sized ones. Both minimums are asserted by test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 9 — colour never alone                  | `ContextStamp` (`packages/ui/src/context-stamp/ContextStamp.tsx`) gives each of the three fix classes — deliberate, ambient, none — a distinct border style (solid vs dashed) _and_ distinct wording, never colour alone; its `fix` prop is a discriminated union on `quality`, so an accuracy-less or wrongly-shaped fix cannot even be constructed. `InputAffordanceRow` applies the same two-channel pattern to completion state: a solid border plus a check mark appended to the label, not a colour change alone. `CaptureDial` (`packages/ui/src/capture/CaptureDial.tsx`) is what this row named the traffic-light frame for before the frame was deleted (spec §9.2): its grade word — `GOOD FIX` / `FAIR FIX` / `POOR FIX` — is always rendered beside the status colour, at every phase, so the colour is never read alone, and on a poor fix a third channel joins them — the accuracy circle's own outline dashes (`field.dialAccuracyOutlineDash`/`dialAccuracyOutlineDashGap`, `packages/tokens/src/scales.ts`), on the accuracy circle specifically because that circle _is_ the accuracy, and a dashed boundary reads as "this edge is uncertain" — precisely what a poor fix means. Good and fair fixes draw a solid outline. Guarded on `grade === 'poor' && !lockedNow` (a poor fix never locks by construction, but `grade` and `locked` are independent props, so the guard forecloses a theoretical dashed-and-locked hybrid rather than relying on that construction alone); asserted by test, including a mutation check that the guard is load-bearing. Spec §9.2.2's "a dashed treatment when poor" is implemented, not merely referenced. `Button` (`packages/ui/src/primitives/Button.tsx`) applies the same rule to destructive confirmation: its `danger` kind gives a destructive action a distinct `backgroundColor` (`theme.colors.statusPoor` — the same token GPS grading uses for "this is the bad one", borrowed rather than adding a parallel red) so it never reads as identical to a `secondary` action beside it, label alone. The capture screen's removal confirmation (`apps/fieldkit/app/capture.tsx`) uses `kind="danger"` on REMOVE and `kind="secondary"` on KEEP IT; asserted by test that the two render different backgrounds, not merely different labels.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 10 — two-line clamp                     | `ProjectName` (`packages/ui/src/names/ProjectName.tsx`) sets `numberOfLines={2}`; asserted by test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 11 — middle truncation                  | `NameChip` (`packages/ui/src/names/NameChip.tsx`) sets `ellipsizeMode="middle"`; asserted by test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 12 — optional short label               | `resolveDisplayName` (`packages/ui/src/names/shortLabel.ts`) returns the trimmed short label when present and falls back to the full name — including its truncation — when blank, rather than attempting to shorten it automatically.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 14 — never animate a name               | Neither `ProjectName` nor `NameChip` has an animation path: both render a single static `Type` element with no `Animated` wrapper, so there is nothing to opt out of.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 16 — spoken labels                      | `Button` (`packages/ui/src/primitives/Button.tsx`) carries a `spokenLabel` prop used as the `accessibilityLabel`, defaulting to the visible `label` when not given. `Screen` (`packages/ui/src/primitives/Screen.tsx`) carries an optional `spokenDescription` prop, rendered on a dedicated zero-size `accessible` node rather than the screen's own container (which would collapse every child into one opaque focus stop) — the component-library support for the "every screen carries a spoken description" half of this rule; the voiced mode itself is a later plan. **A `Screen` that passes a description but no `testID` renders that node with no `testID` either, which is how a screen ends up carrying descriptions no test can reach** — for a while `voice.tsx` was the only screen in the repository whose descriptions were asserted at all, and stripping every one of them off `capture.tsx` and `camera.tsx` left both suites fully green. Both now tag their `Screen`s (`capture-screen`, `camera-screen`) and assert the description in each state they can render; `capture.tsx`'s recorded state builds its description from what is actually attached and from whether a removal question is open, because a sentence naming only the capture number, the accuracy and the ways onward describes the state as it was before media was added to it. `ContextStamp` (`packages/ui/src/context-stamp/ContextStamp.tsx`) exports `composeContextStampSpokenLabel`, which turns its glyph-prefixed chips (`◎ ±4 m`, `~ ±38 m · 4 min old`, `⚑ no position`, `▣ field-s24`) into one readable sentence set as the component's `accessibilityLabel`, covering all three fix classes plus the optional place, device and activity; asserted verbatim by test, including the spec's worked example ("recorded during Survey 3, near Yarra Flats — North Reach, 120 m away").                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 17 — three capture states               | Review only, and it needs to stay a review check: nothing a component can enforce distinguishes "the screen hid its panels" from "the screen had no panels". `apps/fieldkit/app/diagnostics.tsx` is the reference implementation — a `phase` of `ready`/`acquiring`/`recorded` chooses between two layouts and three sets of words for one shared capture frame — and its tests assert the transitions directly: that a tap hides the panels, the logs, the choosers and the record list; that completing brings them back; and that taking another reading hides them again. A capture screen added later without those assertions has not met this rule. One thing the reference implementation gets right that is easy to get wrong: the countdown transcript is hidden during _Acquiring_ but visible the instant the point is recorded, because it is an artefact the work produces, and a state model that loses a measurement to a tidier layout has failed at the thing it was for. `apps/fieldkit/app/capture.tsx` is the field screen built to this rule, and carries the required assertions: a tap leaves one control and the two numbers (`the acquiring state`), completing brings back the capture number, the final accuracy, the sample count, how the wait ended and the four affordances (`the recorded state`), and taking another reading removes all of them again (`takes another reading back to the ready state, with its panels gone again`). Its _recorded_ state names how the wait ended — timer, override or plateau — because spec §9.1.5 was corrected so that a plateau no longer announces itself live, which makes this state the only place that fact ever reaches her.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 18 — counts and genuinely-disabled busy | Component-level, with tests, not lint — there is no lint rule that can read "this label ought to carry a number" out of a JSX tree. `InputAffordanceRow` (`packages/ui/src/inputs/InputAffordanceRow.tsx`) takes a `counts` prop (`Partial<Record<InputAffordanceKind, number>>`) and renders `${label} · ${count}` once `count > 0`, ahead of the plain `${label} ✓` a merely-`completed` kind gets — asserted by test with two different non-zero counts on two different kinds in one render (`shows how many of a kind are attached, not merely that some are`), which a single hardcoded literal could not satisfy both of, and by a second test that a `completed` title with no count stays `Title ✓` rather than `Title · 1` (`says done without a number for a kind that can only happen once`). The `busy` prop sets React Native's own `disabled` — not an `onPress` that returns early, which would still report itself pressable to a screen reader and would still swallow a tap silently — asserted by test that a press on a busy tile never reaches the caller's `onPress` (`refuses a second press while a kind is busy`), that `accessibilityState.disabled` is `true` (`marks a busy affordance disabled to a screen reader, not merely dim`), and that the other tiles stay live while one is busy (`leaves the other affordances live while one is busy`). Both facts are two-channel under rule 9: a busy tile's label reads `${label} · Saving`, not only the `opacity: 0.6` dim, and its `accessibilityLabel` appends `, saving` so the fact reaches a screen-reader user who never sees the dim at all — both asserted by test (`gives busy a word, not merely a dimmer look`, `tells a screen-reader user a save is in flight, not merely disabled`). `apps/fieldkit/app/capture.tsx`'s `RecordedAffordances` is the caller this rule was settled for: it passes `counts={{ photo: photoCount, voice: voiceCount }}` on every render, and `busy` with whichever of `title`/`description` has a save in flight — **not** `photo`/`voice`, because on this screen pressing either of those tiles navigates straight to `/camera` or `/voice` rather than writing anything in place, so they never enter a saving state here at all (the screen's own comment says so, next to where `busy` is built). `InputAffordanceRow` itself is kind-agnostic — its own test suite drives `busy={['photo']}` directly, and the gallery does the same, to prove the _component_ disables whichever kind it is told to, independent of which caller happens to exercise that kind today. A future caller that writes a photo or a voice note in place — Plan 5's media-first entry points, most plausibly — is the one expected to be first to pass `busy={['photo']}` or `busy={['voice']}` for real. |
| 19 — text entry is its own surface      | Component-level, with tests, not lint. `apps/fieldkit/app/capture.tsx`'s `FieldEditor` renders the title/notes box inside a React Native `Modal` — with its own `SafeAreaView` (a `Modal` is a separate Android window and is not inside the one `_layout.tsx` wraps every route in), its own `KeyboardAvoidingView`, and the card top-anchored rather than centred so the box is clear of a keyboard rising from the bottom whether or not the avoidance behaves. It replaced a box that rendered last in the recorded state's column, under the keyboard, which is how the rule was learned: she typed blind and then could not tell whether anything had saved. Pinned by test (`apps/fieldkit/app/__tests__/capture.test.tsx`, `the editor, which is a modal and not the foot of the column`) in the one way that can actually fail — by walking the rendered tree to the input's nearest ancestor and requiring it to be a `Modal` rather than by matching a `testID`, which a plain `<View>` in that column would satisfy identically. Verified by swapping the `Modal` for a `View` and watching those tests name the ancestors they found instead (`View < View < RNCSafeAreaView < ... < RCTScrollView`). The save half is `capture-save-confirmation`, rendered directly under the tile she pressed and built from the record `renameRecord` returned rather than from the draft she typed, so a repository that stored something else cannot be confirmed as having stored what she typed; it is an `accessibilityLiveRegion="polite"` node, because the modal carries its own spoken description and a reader focused inside a surface being removed is otherwise told nothing. Asserted for both kinds, for a cleared box (`Notes cleared`, not `Notes saved:` and a blank), and for its absence after a cancel and after a failed save — the cancel case being what stops the closing surface being mistaken for the confirmation. **What no test here can see:** whether the box is genuinely clear of the keyboard on a device. That is Android window insets, `KeyboardAvoidingView` inside a `Modal` under edge-to-edge, and the modal's own safe area, none of which exists under `jest-expo` — where `Modal` is `@react-native/jest-preset`'s mock and renders its children inline. It stays a device check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 21 — no tile for an unbuilt destination  | Component-level, with tests, not lint — no lint rule can tell a tile whose destination exists from one whose destination does not. `ToolTiles` (`packages/ui/src/launcher/ToolTiles.tsx`) builds its tiles as `order.filter((kind) => available.includes(kind))`, so a kind the caller does not name is not rendered in any form: there is no per-tile `disabled` prop, no badge, and no branch that could draw either. Asserted at both ends, because the component and its caller can fail this rule separately. **Component:** `renders nothing for a tool that does not exist yet` (`packages/ui/src/launcher/__tests__/ToolTiles.test.tsx`) renders `available={['capture', 'records']}` and requires `tool-batching` and `tool-media` to be _absent_ — `queryByTestId(...)` is `null`, rather than a tile found and then inspected for dimness, which is the assertion a disabled-tile implementation would pass just as happily. **Caller:** `offers no tile for a tool that does not exist yet` (`apps/fieldkit/app/__tests__/index.test.tsx`) makes the same two assertions against the real launcher, which is what pins `AVAILABLE_TOOLS` rather than the component's honouring of it. Two of the ordering tests are a second net under the component half, because they assert the exact rendered array rather than a prefix: `puts records first for sampling, where the log leads` and `keeps a stable order when there is no activity to order by` both pass a partial `available`, so a component that started rendering unavailable kinds would fail them on the extra tiles. `floats capture and records for a survey` would not — it checks only the first two — which is why it is not the test this row rests on. |
| Semantic tokens only                    | ESLint rule 1 in `eslint.config.mjs` (`no-restricted-syntax`) rejects raw hex colours — as plain string literals and as non-interpolated template literals — anywhere under `packages/` and `apps/` except `packages/tokens/src/ramp.ts` (the raw material layer itself) and the tokens package's own tests (which pin those raw values on purpose). The same rule also rejects named (`'white'`, `'transparent'`, ...) and functional (`'rgba(...)'`, `'hsl(...)'`, ...) colour literals, scoped to colour-bearing style properties (`backgroundColor`, `borderColor`, `color`, ...) rather than every string literal in the codebase — a blanket ban would misfire on ordinary domain text in an eDNA/biodiversity app (species and site names like "orange-bellied parrot" or "coral reef"). A fifth selector on the same rule closes the remaining gap: a component reaching into `ramp` directly (e.g. `ramp.brand.teal`) is not a colour literal at all, so no literal-matching selector can catch it; importing `ramp` from `@corymbia/tokens` is rejected everywhere except `packages/tokens` itself (structurally exempt — its own files reach `ramp` via a relative import, never the package specifier) and `packages/brand`, whose logo artwork is deliberately theme-invariant and is not supposed to re-theme with the semantic layer. `packages/brand`'s exemption from just that one selector needs its own config block (re-declaring the other four) rather than an entry in RULE 1's `ignores`, because a flat-config `ignores` exempts a file from every selector in the block, not one selector within it — and because two blocks matching the same file can never each contribute a different subset of the same rule's array; the later block replaces the earlier one's entirely.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Size class and device class only        | ESLint rule 2 in `eslint.config.mjs` (`no-restricted-imports`) rejects `useWindowDimensions` and `Dimensions` from `react-native`, both as a named import and via a `react-native/*` subpath import, everywhere except `packages/ui/src/layout/useLayout.ts` — the one function permitted to read the window — and the test mock that stands in for it. `useLayout()` derives two separate vocabularies from that one reading: `sizeClass` (compact/medium/expanded, from current width) drives layout, and `deviceClass` (phone/tablet, from the shortest side) drives ergonomics. Reach zones (`packages/ui/src/layout/reach.ts`) resolve from `deviceClass` and orientation only, never from `sizeClass` — a tablet's corners don't get easier to reach because the window happens to be `expanded`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Tool independence                       | ESLint rule 3 in `eslint.config.mjs` (`import/no-restricted-paths`) rejects imports from a sibling tool under `apps/fieldkit/src/tools/`. It is given an explicit `basePath` (the repo root, derived from the config file's own location) because Turborepo runs each workspace's `lint` script with that workspace as the process's working directory, not the repo root — the plugin resolves its zone paths against `process.cwd()` by default, so without an explicit `basePath` the rule silently matches nothing under a real `turbo run lint` invocation and no error would ever reveal it. This rule needs one zone per tool directory (`except` is a static path, not a back-reference to `target`), which means a human has to remember to mirror the zone when a second tool is added — `scripts/verify-lint-rules.mjs` now enumerates the real directories under `apps/fieldkit/src/tools/` and fails loudly if any lacks a matching zone, so a forgotten mirror is a check failure instead of a silent gap. This rule is currently inert in the sense that no second tool exists yet to violate it — `apps/fieldkit/src/tools/` is not yet populated — but it fires correctly against a fixture, see below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Rotation stays unlocked                 | `apps/fieldkit/app.json` deliberately has no `"orientation"` key. Expo bakes that setting into the Android manifest for every device class; locking it to `portrait` (as the create-expo-app scaffold does by default) would make `sizeClass: 'expanded'` and the `bottomCorners` reach anchor — both produced by `useLayout`/`resolveReach` above, and load-bearing for the tablet workspace — unreachable on a shipped 10-inch tablet build, since that device class only enters `expanded` in landscape. Device verification on a phone would not catch this, because a phone never reaches `expanded` either way. Do not re-add `"orientation": "portrait"` for tidiness.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

All three ESLint rules above are proven to actually fire under the real invocation — not just
configured and assumed to work — by `scripts/verify-lint-rules.mjs`, run with
`pnpm run lint:verify-rules`. It writes small fixture files and lints them by `chdir`-ing into
each workspace directory the way Turborepo's per-workspace `lint` script really does, then
asserts each rule reports what it should: a rejected raw hex literal (string and template
form), a rejected `Dimensions` import (named and subpath form), a rejected cross-tool import,
and — the inverted case that catches a rule too broad to mean anything — an import between two
files in the _same_ tool that must still be allowed. This script is itself part of how these
rules are enforced: a lint rule that has never been proven to fire under the real invocation is
no more trustworthy than a rule enforced by memory.

The same script also enumerates the real directories under `apps/fieldkit/src/tools/` and fails
if any lacks a mirrored `import/no-restricted-paths` zone in RULE 3 — the config-drift check for
the "one zone per tool" limitation described in the Tool independence row above. This check is
read-only against the real config and directory tree (it imports `eslint.config.mjs` itself
rather than re-describing it), separate from the fixture-based rule-firing cases.

Beyond the three architectural rules, `eslint.config.mjs` also enables a standard baseline
(ESLint's own `recommended` rules plus `@typescript-eslint`'s `recommended` rules) and
`eslint-plugin-react-hooks`'s `rules-of-hooks` and `exhaustive-deps`, both bumped from the
plugin's default `warn` to `error` — a warning does not fail `eslint <dir>` in this repo (there
is no `--max-warnings`), so leaving them at `warn` would have been the same "configured but not
enforced" gap this document exists to close. Hook-order and effect-dependency bugs are exactly
the class this repo is about to start writing once GPS subscriptions and fix averaging land, so
this baseline is checked before that work begins, not after the first bug report.

Rules 1, 2, 3, 4, 6, 13 and 15 are judgement calls that no linter can make. They are checked at
review, and every screen review checks all of them. Rule 4 is the partial exception noted in the
table above: it remains a judgement call in general, but its hardest case — the duplicate guard —
is pinned by a test written to fail on the right thing.

## Adding a rule

When a design decision is made that should hold across screens, add it here in the same
session it is made, and note how it will be enforced. If it cannot be enforced by a component
or a lint rule, say so explicitly — that is a signal to watch it in review.
